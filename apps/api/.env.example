# Database
STORAGE_URL=:memory:

# Local development flag (set to TRUE for local dev, omit for production)
IS_LOCAL=TRUE

# Google AI (Gemini)
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key

# AWS Bedrock (for Nova Canvas image generation)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_SESSION_TOKEN=your_session_token

# S3 Storage (Production)
S3_BUCKET_NAME=kimigatari-comics

# MinIO (Local Development) - Use docker-compose up -d
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET=kimigatari-assets
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

LANGFUSE_PUBLIC_KEY=pk-lf-xx
LANGFUSE_SECRET_KEY=sk-lf-xx
LANGFUSE_BASEURL=https://cloud.langfuse.com
